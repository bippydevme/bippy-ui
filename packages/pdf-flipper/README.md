# @bippy-ui/pdf-flipper

```bash
pnpm add @bippy-ui/pdf-flipper
```

```tsx
import { PdfFlipper } from "@bippy-ui/pdf-flipper";

export default function Catalog() {
  return <PdfFlipper src="/catalog.pdf" />;
}
```

`PdfFlipbook` is an alias of `PdfFlipper`.
